# The pose model is the 0.10 series: the 1.0 wheels crash on macOS at start-up,
# and 0.10 has wheels for Python 3.9 to 3.12 only.
mediapipe==0.10.14
numpy>=1.26,<2.2
# pyrealsense2 is not on PyPI for macOS. Install one of these instead, in order:
#   pip install pyrealsense2-macosx      (Apple Silicon, macOS 15 or later)
#   build librealsense from source
# On Linux: pip install pyrealsense2
# realsense-applesilicon installs a different module and will not work here.

pillow>=10
